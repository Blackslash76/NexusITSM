@if (Group != null)
{
    <span style="font-size:10px;padding:1px 6px;border-radius:3px;background:@(Group.Color)18;color:@Group.Color;font-weight:600;white-space:nowrap">
        @Group.Icon @Group.Name
    </span>
}

@code {
    [Parameter] public SupportGroup? Group { get; set; }
}
